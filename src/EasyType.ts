// I couldn't make this type work yet.
// But if possible, I would like to create it
// then, if it's possible, augment it to check for problems
// that might happen in the transform phase
export type EasyType = unknown;
